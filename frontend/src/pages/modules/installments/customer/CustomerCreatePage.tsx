import type { ReactNode } from 'react'
import CustomerFormPage from './CustomerFormPage'

/**
 * Create Customer page — renders the shared form in create mode.
 */
export default function CustomerCreatePage(): ReactNode {
    return <CustomerFormPage />
}
