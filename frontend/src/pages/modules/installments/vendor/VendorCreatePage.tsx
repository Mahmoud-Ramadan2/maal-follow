import type { ReactNode } from 'react'
import VendorFormPage from './VendorFormPage'

/**
 * Create Vendor page — renders the shared form in create mode.
 */
export default function VendorCreatePage(): ReactNode {
    return <VendorFormPage />
}

