import type { ReactNode } from 'react'
import PurchaseFormPage from './PurchaseFormPage'

/**
 * Create Purchase page — renders the shared form in create mode.
 */
export default function PurchaseCreatePage(): ReactNode {
    return <PurchaseFormPage />
}
